<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_welcome" >

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_marginLeft="15dp"
        android:orientation="vertical" >

        <ImageView
            android:id="@+id/login_logo"
            android:layout_width="240dp"
            android:layout_height="48dp"
            android:contentDescription="@string/welcome_pic_desc"
            android:src="@drawable/login_logo"
            android:text="@string/app_name_lower"
            android:textColor="@android:color/white"
            android:textSize="60sp" />

        <TextView
            android:id="@+id/btn_apikey"
            android:layout_width="130dp"
            android:layout_height="17dp"
            android:background="@drawable/btn_apikey"
            android:text="@string/btn_apikey"
            android:gravity="right|center_vertical"
            android:paddingRight="20dp"
            android:layout_marginTop="15dp"
            android:layout_marginLeft="10dp"
            android:textSize="12sp"
            android:clickable="true"
            android:onClick="changeApikey" />
    </LinearLayout>

    <ImageView
        android:id="@+id/login_btn_enter"
        android:layout_width="25dp"
        android:layout_height="50dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="15dp"
        android:background="@drawable/login"
        android:contentDescription="@string/welcome_pic_desc"
        android:onClick="login" />

</RelativeLayout>